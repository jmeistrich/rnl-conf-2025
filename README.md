# React Native London Conf Talk

This talk is about React Native macOS

# Run the slides

```bash
cd slides
bun i
bun start
```

Then open http://localhost:3030/ or http://localhost:3030/presenter/ to see the notes.
